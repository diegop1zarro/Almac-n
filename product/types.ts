// declaramos el tipo de datos que vamos a recibir

export interface Product {
    id: string;
    title: string;
    catergory: string;
    description: string;
    image: string;
    price: number;
}